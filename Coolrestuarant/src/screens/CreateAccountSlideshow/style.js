import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  dotStyle: {
    borderWidth: 0.5,
    borderColor: '#7F7F7F',
  },
  swiperViewContainer: {
    flex: 1,
    zIndex: 3,
  },
});

export default styles;
